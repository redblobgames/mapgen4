{
    "name": "@redblobgames/dual-mesh",
    "license": "Apache-2.0",
    "version": "3.0.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "dependencies": {
        "delaunator": "^5.0.0"
    },
    "devDependencies": {
        "poisson-disk-sampling": "^2"
    },
    "scripts": {
        "test": "node tests.js",
        "dist": "tsc",
        "libs": "tsc; echo \"export {default} from 'poisson-disk-sampling';\" | esbuild --bundle --minify --format=esm --outfile=build/_poisson-disk-sampling.js ; esbuild dist/create.js --bundle --sourcemap --format=esm --outfile=build/_meshbuilder.js"
    }
}
